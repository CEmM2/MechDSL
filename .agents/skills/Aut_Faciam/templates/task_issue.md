## <task_id>: <task_title>

**Phase:** <N> — <phase_name> (see #<phase_issue_number>)
**Plan source:** `<plan_file>` lines <plan_lines>
**Task JSON:** `<tasks_folder>/json/<task_id>.json`
**Tier:** <test_plan.tier>

### Objective
<task.objective>

### What This Task Does (Plan Context)
<!-- Describe where in the plan this task originates. Reference specific plan sections
     and line numbers from plan_lines and plan_assets. Explain how this task fits into
     the phase's goals — what comes before it, what depends on it. -->

### Implementation Steps
1. <step from task.implementation_steps>
2. <step>
3. ...

### Deliverables
- <deliverable from task.deliverables>
- ...

### Acceptance Criteria

#### Existing tests (covered by scaffold)
<!-- For each test_artifact that covers existing cases: -->
- [ ] `<test_file_path>::<test_function>` — <what it verifies>

#### Test stubs (generated by scaffold)
<!-- For each stub generated: -->
- [ ] `<stub_file_path>::<test_function>` — <what it verifies>

#### Additional criteria
<!-- From task.acceptance_criteria, excluding those already covered by tests above.
     Also include relevant criteria from design docs, plan constraints, conventions,
     and lessons learned from previous phases (if handoff exists). -->
- [ ] <criterion>
- [ ] <criterion>

### Verification Commands
```bash
<each command from task.verification_commands>
```

### Risks
- <risk from task.risks>

### Blocked By
<!-- If task.blocked_by is non-empty: -->
- Blocked by #<issue_number_of_blocker> (<blocker_task_id>: <blocker_title>)
<!-- Else: -->
None — can start immediately

### Agent Dispatch
```
Read issue #<this_issue_number> and implement it.
Task JSON: <tasks_folder>/json/<task_id>.json
```
