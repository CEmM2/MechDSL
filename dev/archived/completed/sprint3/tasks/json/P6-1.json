{
    "task_id": "P6-1", "title": "Ruff lint and format pass", "phase": "6",
    "objective": "Run ruff check --fix and ruff format across all packages, fix any remaining issues.",
    "plan_file": "dev/plans/sprint3.md", "plan_lines": ["247"],
    "plan_assets": [], "blocked_by": [], "blocks": ["P6-3"],
    "scope": ["Run uv run ruff check --fix packages/", "Run uv run ruff format packages/", "Verify clean pass"],
    "implementation_steps": ["Run ruff check --fix", "Run ruff format", "Verify both pass cleanly"],
    "deliverables": ["Clean ruff check and format pass"],
    "acceptance_criteria": ["uv run ruff check packages/ exits 0", "uv run ruff format --check packages/ exits 0"],
    "verification_commands": [
        "uv run ruff check packages/",
        "uv run ruff format --check packages/",
        "uv run pytest packages/mechdsl-core/tests/test_phase6_exit.py::TestTaskP6T1 -v"
    ], "test_plan": {"tier": "unit", "cases": ["ruff check", "ruff format --check"]},
    "test_artifacts": ["packages/mechdsl-core/tests/test_phase6_exit.py"], "risks": [
        "ruff check --fix may rewrite import blocks and dead-code paths across both workspace packages, so the cleanup can extend beyond files touched directly in Sprint 3.",
        "Formatting cleanup can expose latent typing or test breakage if stale helper code and unused branches are removed at the same time."
    ],
    "status": "done", "completion_date": "2026-04-12",
    "test_completion": {"passed": 4, "total": 4, "pass_rate": 100, "commands": [
        {"command": "uv run ruff check packages/", "passed": 1, "total": 1},
        {"command": "uv run ruff format --check packages/", "passed": 1, "total": 1},
        {"command": "uv run pytest packages/mechdsl-core/tests/test_phase6_exit.py::TestTaskP6T1 -v", "passed": 2, "total": 2}
    ]},
    "review_score": 9, "review_breakdown": {"minor": 1, "medium": 0, "high": 0, "critical": 0}, "review_status": "approved",
    "implementation_branch": "SOSOVSKI/phase6-exec", "completion_notes": ["Ran ruff format across the package tree, resolved the Python 3.12 type-alias lint requirement in tensor_ops.py, and left ruff clean across both workspace packages."],
    "github_issue": {"phase_issue": 20, "task_issue": 44, "repo": "SOSOVSKI/MechDSL"}
}
