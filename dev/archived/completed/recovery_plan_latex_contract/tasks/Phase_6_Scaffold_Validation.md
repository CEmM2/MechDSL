# Phase 6 Scaffold Validation

Generated by `/Aut_Faciam scaffold 6 dev/plans/recovery_plan_latex_contract.md` on 2026-04-29.

Plan: `dev/plans/recovery_plan_latex_contract.md` — Phase 6 (R5) "Integrate `algo2code` at the least risky seam".
Tasks folder: `dev/tasks/recovery_plan_latex_contract/`.
Tracker: `dev/tracking/tasks-tracker_recovery_plan_latex_contract.md`.

## Field validation

| Task ID | Title | Missing / Incomplete Fields | Scaffold Action |
|---------|-------|-----------------------------|-----------------|
| P6-1 | Add an optional `algo2code`-generated PCG path behind `LinearSolverInterface`. | `verification_commands` empty; `test_artifacts` empty | auto-filled (commands + stub path appended in Step 4) |
| P6-2 | Keep the current imported solver path as the default fallback until generated PCG is stable. | `verification_commands` empty; `test_artifacts` empty; `deliverables` vague ("Changes to same"); `risks` generic placeholder | auto-filled for commands/artifacts; **needs-human-review** for `deliverables` (clarify: same surfaces as P6-1 — `solver/import_adapter.py`, solver integration layer, `algo2code` interface hook) |
| P6-3 | Add a single stable integration test for `algo2code` → PCG → Newton solve plumbing. | `verification_commands` empty; `test_artifacts` empty | auto-filled |
| P6-4 | Defer radial-return replacement until frontend + IR alignment is settled. | `verification_commands` empty; `test_artifacts` empty; `risks` generic placeholder | auto-filled |
| P6-5 | Document `algo2code`'s role in the recovered architecture to prevent renewed drift. | `verification_commands` empty; `test_artifacts` empty; `risks` generic placeholder | auto-filled |

Notes:
- All `objective`, `acceptance_criteria`, `implementation_steps`, `test_plan.tier`, and `test_plan.cases` fields are present and non-placeholder.
- `risks` placeholder strings ("No specific risk attributed in the plan; standard care applies.") are tolerated — the plan itself does not enumerate per-task risks for P6-2/P6-4/P6-5.
- `deliverables` for P6-2 reads "Changes to same" — this refers back to P6-1's surfaces (`solver/import_adapter.py`, solver integration layer, `algo2code` interface hook) per the recovery plan table at `dev/plans/recovery_plan_latex_contract.md:318`. Flagged for human confirmation before execution.

## Pre-scaffold context

- Predecessor: Phase 5 closed at commit `838754a` (PR #181 merged). Aggregate suite: **1629 pass / 81 skipped / 110 deselected / 0 failed**.
- Cross-phase blocker resolved: P4-1 (ElementIR enrichment) is `done` — P6-1 is unblocked.
- New pytest markers introduced in Phase 5: `stable_backend` / `experimental_backend` are codegen-scoped — Phase 6 solver tests must NOT carry them.
- Solver adapter contract anchored at `packages/mechdsl-core/src/mechdsl/solver/import_adapter.py:26-57`. P6-1 must add a third concrete adapter (e.g. `Algo2CodePCGSolver`) without modifying `LinearSolverInterface`, `CGSolver`, `PCGSolver`, or `ScipyCGSolver`.

## Scaffold Summary

| Metric | Value |
|--------|-------|
| Tasks scaffolded | 5 |
| Test cases assessed | 10 |
| Cases covered by existing tests | 0 |
| Cases partially covered (stubs generated) | 0 |
| Cases with no existing tests (stubs generated) | 10 |
| New stub files created | 5 |
| Total new stubs generated | 10 |
| Tasks fully covered by existing tests (no stub needed) | 0 |
| Tasks needing human review | 1 (P6-2 deliverables) |
| Auto-filled fields | `verification_commands`, `test_artifacts` (all 5 tasks) |

## Existing Test Coverage Found

| Task ID | Test case | Existing test file | Function | Coverage |
|---------|-----------|--------------------|----------|----------|
| — | — | — | — | none |

No prior solver-adapter / algo2code-integration / radial-return-deferral / monorepo-architecture-doc tests exist in either package's test tree. All 10 cases are `missing` and have fresh stubs.

## Tasks Needing Human Review Before Execute

| Task ID | Title | Field | Issue |
|---------|-------|-------|-------|
| P6-2 | Keep the current imported solver path as the default fallback until generated PCG is stable. | `deliverables` | Reads "Changes to same" — refers to P6-1's surfaces (`solver/import_adapter.py`, solver integration layer, `algo2code` interface hook) but should be made explicit before execution to avoid ambiguity in the gate-A spec-compliance check. |

## Ready for Execute

Fully scaffolded:
- P6-1: Add an optional `algo2code`-generated PCG path behind `LinearSolverInterface`.
- P6-3: Add a single stable integration test for `algo2code` → PCG → Newton solve plumbing.
- P6-4: Defer radial-return replacement until frontend + IR alignment is settled.
- P6-5: Document `algo2code`'s role in the recovered architecture to prevent renewed drift.

Needs human review before execution:
- P6-2: Keep the current imported solver path as the default fallback until generated PCG is stable. — clarify `deliverables` (currently "Changes to same"; resolve to P6-1 surfaces explicitly).

## Per-task stub inventory

- `tests/plan_tests/recovery_plan_latex_contract/test_p6_1.py` (4084 B) — class `TestP6_1`, 2 tests, marker `integration`.
- `tests/plan_tests/recovery_plan_latex_contract/test_p6_2.py` (4564 B) — class `TestP6_2`, 2 tests, no marker (`unit` not registered; `--strict-markers` enforced).
- `tests/plan_tests/recovery_plan_latex_contract/test_p6_3.py` (5420 B) — class `TestP6_3`, 2 tests, marker `integration`.
- `tests/plan_tests/recovery_plan_latex_contract/test_p6_4.py` (2935 B) — class `TestP6_4`, 2 tests, marker `regression` (docs not registered).
- `tests/plan_tests/recovery_plan_latex_contract/test_p6_5.py` (4276 B) — class `TestP6_5`, 2 tests, no marker.
